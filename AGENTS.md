# 杀尾-修复版2：项目规则

本文件继承父目录 `C:\Users\Administrator\Desktop\每天工具\爬虫合集\AGENTS.md`，只规定本项目独有的事实和执行差异。父级规则继续负责授权、来源隔离、数据正确性、缓存事务、输出安全和验收门禁；本文件不重复它们。

## 适用范围

- 项目根目录：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2`
- 配置、解析、抓取、校验和持久化代码位于项目根目录的 `shawei\`。
- 正式输出不写入项目根目录，也不写入旧版目录。
- 新增站点或修复失败站点时，只处理用户明确指定的站点、URL、期数、方向和字段；不得顺带跑全站或改动其他站点。普通全站单期运行只有在用户明确要求时才执行。

## 数据契约

- 普通站点结果必须是一个 `0` 至 `9` 的尾数。
- 双尾站点只能由 `shawei\config\constants.py` 的 `TWO_TAIL_SITE_URLS` 按 URL 授权，结果必须是两个合法尾数；不能按名称或页面文字临时放宽。
- 站点身份由名称、规范化 URL、topic 或文章 ID、栏目和方向共同确定。新增或修复不能只凭名称或尾数判断身份。
- 合法值、尾数数量和最终结果格式由 `shawei\validation\validator.py` 与项目配置共同裁决；“绝杀一尾”等标签本身不是结果值。

## 方向和专属规则

- `top` 和 `bottom` 是同一权威文档、同一区块内的方向边界，不是全页面的最大或最小期号。
- `top` 目标期必须是目标区块第一条有效目标记录；`bottom` 目标期必须是最后一条有效目标记录。窗口由配置决定，运行时不得调整。
- 无效字段或占位行是否构成边界障碍由该 URL 的专属规则决定；不能在运行时凭经验删除障碍或用另一方向补位。
- `https://156.225.88.144:12098/#234432` 的“开奖发财【综合杀料】”规则明确把精确值为 `?尾` 的未发布行排除在 bottom 边界之外；其他无效值仍按专属规则处理。
- 同一期多记录站点由 `shawei\config\rules.py` 的 `SAME_PERIOD_RECORD_SELECTION_URLS` 管理：必须扫描完整专属区，去除同一内容的重复展示，按区块顺序编号，再按配置方向选择；不同记录不能互相替代。
- 默认窗口和通用排除规则在 `shawei\domain\defaults.py`；URL 级规则在 `shawei\config\rules.py`。规则变更必须绑定 URL、topic 或稳定文章 ID。

## 抓取、解析和动态页面

- 抓取层只取得原始文档；解析器只生成候选和证据；统一校验器负责期数、方向、栏目、数量、合法值、边界和冲突裁决；解析器和抓取层不得写正式文件。
- 专属解析器失败时不能回退通用解析器凑成功。主要实现位置：`shawei\fetch\`、`shawei\parsers\`、`shawei\validation\validator.py`。
- 每条有效候选都应能追溯到来源 URL/方式、站点或文章 ID、锚点、区块边界、原始期数和数据行、区块顺序及解析器 ID。
- 普通站点按 `effective_rule_for()` 的 `prefer_rendered` 固定选择 HTTP 或浏览器；HTTP 能完整取得目标数据时不额外启动浏览器。需要浏览器的站点复用已有浏览器上下文。
- `/article/admin/`、`/article/manager/`、`/article/lottery/` 及配置的同类动态页面，必须先按 URL 中的记录 ID 定位专属接口对象；只有接口 404 或空壳且专属规则允许时才能浏览器兜底，兜底后仍要重新核对同一 ID、作者、栏目和正文。

## 正式入口

- 单期：`shawei_crawler.py --period <期数>`
- 多期：`shawei_multi_period_crawler.py --periods <期数...>`；每期独立处理并强制不更新正式缓存。
- 失败站独立验证：`shawei_failed_site_validator.py`。该脚本当前只遍历文件内 `TARGETED_FAILED_SITE_CHECKLIST` 的固定站点和期数，不能把它当作任意用户指定站点的通用验证器。
- 新增站判重：导入 `shawei_duplicate_checker.py` 的服务函数 `evaluate_new_site_admission(name, url, pick, period, periods=10, timeout=20)`；该文件没有独立 CLI。
- 正式连续判重：`shawei_consecutive_duplicate_checker.py`，只读取有效正式缓存，不实时抓取、不写缓存；支持 `--period`、`--output`、`--recent-cache`、`--cache-only`。

对应 BAT：

- `爬虫-每天杀尾.bat`
- `爬虫-每天杀尾-多期不更新缓存.bat`
- `爬虫-每天杀尾 - 检测重复.bat`

## 缓存和输出

- 配置：`sites.json`。
- 正式缓存：`recent_10_cache.json`，固定保存最近 10 期倒序数据，并绑定当前配置指纹、站点身份、方向和失败状态。
- 健康状态：`shawei_site_health.json`；它只能记录健康结果，不能代替成功 TXT、失败 TXT 或缓存。
- 成功输出：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳\<期数>期-尾.txt`
- 失败输出：`C:\Users\Administrator\Desktop\每天工具\爬虫合集\七类数据统一归纳失败\<期数>期-尾-失败.txt`
- 路径由 `shawei\config\paths.py` 统一解析；正式输出路径变更必须同时更新项目规则和相关测试。
- 单期实时判定只能使用本次抓取文档。判定完成、TXT 和健康状态定稿后，才允许滚动更新缓存；缓存不能反向改变本期结果。
- 多期、审计、影子和独立验证不得更新正式缓存。缓存身份、指纹、窗口或站点顺序不匹配时，报告缓存更新/判重未完成，不得强写。
- 成功 TXT 只接收完整校验通过的结果；失败 TXT 必须记录站点、URL、方向、期数、阶段和原因。
- 定向新增或修复时，成功 TXT 只在原结果区追加指定站点并保留原有值和顺序，排行榜放在结果区之后重新统计；失败 TXT 只清理或追加指定站点的当期记录；缓存只更新指定站点的当期结果，其他站点保持不变。
- 文件写入使用项目现有的锁、同目录临时文件和原子替换实现；不得产生或遗留 `.lock`、`.txt.lock`、`.json.lock` 旁路文件。

## 判重和修复门禁

- 新增站点按具体共同期号比较。连续 1 至 2 期相同不算重复，连续 3 至 5 期只能标记疑似并等待用户明确授权，连续 6 期及以上拒绝加入。缺期、失败、无共同期号或缓存无效时判重未完成。
- 不足 10 期只能在用户明确批准本次特例后加入；特例只放宽历史数量，不放宽身份、来源、方向、栏目、边界、字段或冲突规则。
- 失败站修复分两步：用户说“开始验证”时只真实复现和隔离验证，不改正式配置、TXT 或缓存；用户再次说“开始正式更新”后，才迁移已验证的最小改动并跑目标站点正式单期流程。
- 诊断或审计入口不得把截图、旧 TXT、缓存旧值或其他站点数据当作本期成功证据。

## 验证要求

- 数据操作任务按用户指定的站点和期数执行，不因项目规则自动扩展为全站运行。
- 代码修改遵守父级验收范围：局部站点、解析器或输出修改运行受影响测试、Python 编译/静态检查和真实指定期流程；共享流程、统一校验、缓存、存储或入口修改再运行项目完整测试。只读核对、解释和纯文档修改不触发代码测试。
- 只读解释、失败原因说明和缓存状态核对不自动授权修复；已明确授权的定向修改应直接完成并做相应核验。
- 报告必须区分“真实验证通过”“抓取失败”“缓存更新失败”和“未完成”，不能用计划或局部解析测试代替正式流程结论。
