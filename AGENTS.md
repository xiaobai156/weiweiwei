# 杀尾-修复版2项目专属规则

本项目继承上级 `..\AGENTS.md`。本文件只补充“杀尾-修复版2”的数据、路径、入口、方向、缓存、判重和专属实现差异，不重复上级统一规则。

## 项目根目录

项目根目录为：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2`

正式生产输出不保存到项目根目录，也不保存到旧版目录。

## 数据类型与合法值

- 普通结果是一个 `0-9` 的合法尾数。
- 明确授权的双尾站点结果必须是两个 `0-9` 尾数，格式为 `数字、数字`。
- 除以下四个明确授权站点外，所有站点必须严格输出一个尾数：

  - `强烈招牌`
  - `华丽恶梦`
  - `专注凯子`
  - `唯一火势`

- 双尾授权由以下四个 URL 绑定，不得按站名或页面内容临时放宽：

  - `https://qvuuqqs.8imf7-hteuh-ylwuqv.xyz/#/users/1203`
  - `https://qvuuqqs.8imf7-hteuh-ylwuqv.xyz/#/users/3753`
  - `https://qvuuqqs.8imf7-hteuh-ylwuqv.xyz/#/users/46140`
  - `https://qvuuqqs.8imf7-hteuh-ylwuqv.xyz/#/users/5976`

- `0-9` 合法性、单双尾数量和结果格式由统一校验器裁决。
- “绝杀1尾”等标签文字不是尾数；只有目标字段中的实际数字可以作为结果。

## 正式站点配置

正式配置文件：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\sites.json`

当前配置包含 240 个站点。每条配置至少包含：

- `name`
- `url`
- `pick`

`pick` 允许使用：

- `top`、`顶部`、`上`
- `bottom`、`尾部`、`底部`、`下`

配置中的 `name`、URL、topic、文章记录ID、栏目和方向共同构成站点身份。新增或修复不得仅凭名称或尾数字段判断身份。

## top/bottom 方向和窗口

站点配置的 `pick` 是候选选择的最高优先级。

- `top`、`顶部`、`上`：只允许同一权威文档、同一目标区块内靠前的有效候选。
- `bottom`、`尾部`、`底部`、`下`：只允许同一权威文档、同一目标区块内靠后的有效候选。
- `top`指定期的“期数+目标字段”必须是目标区块绝对第一条；前面存在任何“期数+目标字段”记录都必须失败。
- `bottom`指定期的“期数+目标字段”必须是目标区块绝对最后一条；后面存在任何“期数+目标字段”记录都必须失败。
- 结构上已经属于目标字段的无效或占位记录仍是位置障碍；边界记录不合格必须失败，禁止删除后让其他同期期数补位。
- 不能扫描全页面后直接取最大期号。
- 不能用另一方向的候选替代当前方向。
- 期数、站名或栏目、关键词、数据字段必须与同一文档和同一区块绑定。
- 同一目标区块中存在多个不同合法结果时必须失败，除非该 URL 的专属规则明确允许按方向选择。
- 手动指定期数不是当前方向的绝对边界记录时必须失败。

### 同期期多记录专属规则

当前有48个URL被明确配置为“同一期多记录编号模式”（名单由
`shawei/config/rules.py` 的 `SAME_PERIOD_RECORD_SELECTION_URLS` 持有）。

- 单期抓取时必须扫描完整专属区，收集指定期数的全部真实、独立记录。
- 同一条内容在接口、DOM、浏览器或脚本中重复呈现只算一条；不同区块或不同文章记录分别保留。
- 按专属区从上到下编号：第一条为编号1，随后递增。
- `top` 固定选择编号1，`bottom` 固定选择最后一个编号；禁止用其他编号替代。
- 指定期数还必须是当前方向的绝对边界记录；否则失败。
- 选中记录仍必须经过统一的期数、栏目、字段、方向、边界、来源和冲突校验；同一编号记录的跨来源不一致仍失败，不同编号的独立记录按本专属规则选择，不互相替代。
- 运行诊断记录“编号/总数”，正式成功TXT仍沿用原有格式。

默认窗口和通用排除规则位于：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\shawei\domain\defaults.py`

URL 专属规则位于：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\shawei\config\rules.py`

## 专属解析

解析器必须通过 URL、topic 或稳定文章记录ID绑定专属规则。专属规则负责确认：

- 目标站名或栏目锚点；
- 核心杀尾关键词；
- 期数位置；
- 目标字段；
- top/bottom 方向；
- 广告、评论、相邻栏目和无关期数的排除边界。

当前专属解析器和注册表位于：

- `shawei\parsers\dedicated.py`
- `shawei\parsers\topic.py`
- `shawei\parsers\registry.py`
- `shawei\config\rules.py`

专属解析失败时不得回退通用解析器凑成功。解析器只能生成候选和来源证据，最终成功必须经过：

`shawei\validation\validator.py`

## 动态文章和记录ID

以下动态页面必须先按 URL 中的文章记录ID定位：

- `/article/admin/`
- `/article/manager/`
- 项目已配置的同类动态文章页面

处理要求：

- 优先请求同一记录ID的专属接口。
- API必须唯一命中 URL 中的目标记录ID。
- 标题、作者、栏目、正文、期数和尾数必须属于同一接口对象。
- 聚合接口、重复ID、错误ID、作者不一致、空壳或多记录冲突必须失败。
- 只有 HTTP 404，或 HTTP 200 且目标记录为空壳时，才允许浏览器渲染兜底。
- 浏览器兜底后仍必须重新验证同一记录ID。
- 普通站点不得无条件进入浏览器。

动态ID只读审计入口：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\audit_dynamic_article_ids.py`

其报告文件为：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\dynamic_article_id_audit_report.json`

审计不得修改正式缓存、正式配置或正式成功/失败 TXT。

## 正式入口

主入口：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\shawei_crawler.py`

正式业务代码位于：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\shawei\`

单期入口：

`shawei_crawler.py --period <期数>`

单期默认在实时成功/失败 TXT 和健康结果定稿后更新 `recent_10_cache.json`；使用 `--no-update-cache` 时不得更新正式缓存。

单期实时抓取、解析和统一校验只依据本次抓到的目标文档。实时判定完成前不得读取或使用 `recent_10_cache.json` 补数、判期、判方向、解冲突或决定成功/失败。缓存写入是结果后的持久化；缓存更新阶段读取旧 JSON 只用于滚动保留历史窗口，不能反向改变本次实时结果。该 JSON 的业务读取用于新增站点正式重复检测。

多期入口：

`shawei_multi_period_crawler.py`

多期必须逐期执行单期逻辑，并强制使用 `--no-update-cache`。多期不得更新正式缓存。

失败站独立验证入口：

`shawei_failed_site_validator.py`

失败站验证只能真实复现、分析和报告，不得修改正式配置、缓存、成功 TXT 或失败 TXT。

重复检测分为两种用途：

- `shawei_duplicate_checker.py` 只是兼容导入外壳，没有 `main` 或命令行参数，不能作为可执行 BAT/CLI。新增站判重调用其服务函数 `evaluate_new_site_admission(name, url, pick, period, periods=10, timeout=20, workers=8)`。
- `shawei_consecutive_duplicate_checker.py` 才是全站连续重复检测 CLI，支持 `--period`、`--window`、`--consecutive`、`--timeout`、`--workers`、`--output`、`--fail-output`、`--recent-cache`、`--use-recent-cache`、`--no-write-cache` 和 `--cache-only`。

重复检测必须优先使用正式 `recent_10_cache.json`。新增站判重允许从当前缓存中跳过不完整站点，只比较其余完整 10 期向量；正式全站连续检测则会在 `fail_count != 0` 或 `site_count != vector_count` 时整体拒绝缓存。两种口径不得混用，缓存无效、缺期或配置快照不一致时只能报告未完成。

## 正式 BAT

单期抓取：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\爬虫-每天杀尾.bat`

多期抓取，不更新缓存：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\爬虫-每天杀尾-多期不更新缓存.bat`

重复检测：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\爬虫-每天杀尾 - 检测重复.bat`

## 正式缓存

正式缓存文件：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\recent_10_cache.json`

当前缓存字段包括：

- `schema`
- `period`
- `window`
- `site_count`
- `vector_count`
- `fail_count`
- `fail_lines`
- `sites`

每个 `sites` 条目还必须检查站点级 `failed_periods` 和 `failure_reasons`。不能只看顶层 `fail_lines`；当前缓存顶层 `fail_lines` 为空，但 9 个不完整站点正是通过这两个站点级字段记录失败期和具体原因。

缓存窗口固定为最近 10 期，期数按当前期倒序排列。

当前缓存存在部分失败状态时：

- 失败站点不得用旧值冒充当前成功。
- 新增站判重服务可跳过失败或不完整站点，只比较具备完整有效 10 期向量的其余站点。
- 正式全站连续检测不能跳过后继续宣称全站完成；当前实现会整体拒绝含失败或数量不完整的缓存。
- 没有完整有效向量时，判重未完成。
- 缓存与当前 `sites.json` 的名称、URL或方向快照不一致时，判重未完成。
- 单期成功和失败都必须在实时结果定稿后按项目缓存契约滚动更新；失败不能用旧值冒充本期成功。
- 单期实时判定不依赖磁盘缓存；缓存更新失败不得撤销或改写已经生成的成功/失败 TXT，但命令必须明确报错并返回失败状态。
- 多期、历史回抓、审计和独立失败验证不得更新正式缓存。
- 缓存写入必须使用锁、同目录临时文件和原子替换。

当前源缓存为 211 期、10 期窗口、240 个配置站点、231 个有效向量和 9 个失败站点；因此不能把当前缓存宣称为全站完整有效基准。

## 正式输出

成功输出目录：

`C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳`

失败输出目录：

`C:\Users\Administrator\Desktop\每天工具\数据系列\七类数据统一归纳失败`

以上是项目要求的正式目录；当前 `shawei/config/paths.py` 实际使用 `ROOT_DIR.parent`，会解析到 `C:\Users\Administrator\Desktop\新建文件夹 (2)\七类数据统一归纳` 及其失败目录。路径修复和完整流程验收前，不得把当前入口宣称为已写入上述正式目录。

单期成功文件名：

`<期数>期-尾.txt`

单期失败文件名：

`<期数>期-尾-失败.txt`

多期失败汇总文件名：

`<期数1>-<期数2>-...-<期数N>多期汇总失败.txt`

连续重复检测成功文件名：

`<期数>期连续重复检测.txt`

连续重复检测失败文件名：

`<期数>期连续重复检测_失败.txt`

成功 TXT 只能写入完整统一校验通过的结果。失败 TXT 必须保留站点、URL、方向、期数、失败阶段和具体原因。

## 判重阈值

新增站点和正式连续重复检测必须遵守：

- 连续 1-2 期相同：不算重复。
- 连续 3-5 期相同：疑似重复，暂停并人工确认。
- 连续 6 期及以上相同：确定重复，拒绝接收。

比较必须按具体相同期号对齐：

- 不按页面位置硬比。
- 不跨缺失期拼接连续重复。
- 顺序型数据必须比较完整内容和原始顺序。
- 双尾站点与单尾站点不得混用比较。
- 候选为空、抓取失败、无共同期号或缓存无效时，判重未完成。
- 不能把部分相同、顺序不同或长度不同判为整串重复。

## 新增站点专属要求

新增站点必须使用：

- `sites.json`
- `recent_10_cache.json`
- 正式重复检测器
- URL/topic级专属解析器
- 真实页面或接口原始内容

新增站点不得仅凭截图、旧 TXT、临时指纹或页面自行推断结果。

候选站必须：

- 通过名称、URL、topic、文章ID、栏目和字段重复检查；
- 按指定方向抓到缓存最新期或上一期；
- 至少获得一条真实有效数据；
- 逐期验证期数、方向、字段、数量和合法值；
- 按共同具体期号完成正式判重；
- 连续 6 期及以上相同直接拒收；
- 连续 3-5 期相同只能标记疑似，等待人工确认；
- 未完成真实访问、专属解析或正式判重时不得写入 `sites.json`。

不足 10 期只能作为上级规则允许的受限特例处理，必须记录实际有效期数、缺失期数和授权范围；不得放宽方向、字段、数量、来源、边界或冲突规则。

当前 `find_existing_site()` 只比较规范化名称或完全相同 URL，不会自动检查 topic、文章 ID、栏目和字段身份；这些检查必须另行完成并留证。`evaluate_new_site_admission()` 在只有部分真实历史时也可能返回 `accepted=True`，该返回值只代表现有代码判重结果，不等于用户已授权加入；不足 10 期仍必须取得本次明确特例授权后才能写入配置。

## 失败站修复专属要求

现有失败站验证入口：

`shawei_failed_site_validator.py`

该入口不是通用验证 CLI：它只接受 `--timeout`，固定遍历 `TARGETED_FAILED_SITE_CHECKLIST` 中的 21 个站点和各自硬编码期数。用户指定的其他站点或期数不能因为运行该脚本而宣称已验证；必须在隔离环境增加对应定向验证并保留证据。

验证范围至少包括：

- 指定目标期；
- 相邻期和明确不存在的期数；
- 当前方向边界；
- 错栏目和错字段；
- 跨文档借锚点；
- iframe、script和多文档干扰；
- 标题与正文分离；
- 同期冲突；
- 动态文章ID；
- 适用时的浏览器兜底；
- 正式单期入口；
- 正式失败 TXT；
- 正式缓存更新或隔离保护。

当前固定清单验证器只覆盖每站一个硬编码期数，没有自动覆盖相邻期和明确不存在期，因此单独运行它不满足上述完整修复门槛。

单个正则、单个解析函数、模拟文本或局部测试通过，不得视为修复完成。

## 站点健康状态

正式健康状态文件：

`C:\Users\Administrator\Desktop\每天工具\爬虫合集\杀尾-修复版2\shawei_site_health.json`

健康状态只能记录抓取结果、失败阶段和失败原因，不能替代正式结果、缓存或统一校验。

## 源实现与本项目规则的已知冲突

以下内容是当前源实现事实，不得伪装为已满足项目规则：

1. `shawei_duplicate_checker.py`、`shawei_consecutive_duplicate_checker.py`、`shawei_multi_period_crawler.py`以及 `audit_dynamic_article_ids.py`仍存在对根兼容入口 `shawei_crawler.py` 的导入或调用。它们只能作为兼容或诊断入口使用；V2内部新增代码不得反向依赖该入口。

2. `shawei_multi_period_crawler.py`通过 `shawei_crawler.py` 子进程执行多期任务。该实现必须继续保证每一期传入 `--no-update-cache`，不得把多期入口改成正式缓存更新路径。

3. `audit_dynamic_article_ids.py`默认将审计报告写入项目根目录。该报告不得被当作正式业务输出，审计过程不得修改正式缓存、正式配置或正式 TXT；需要隔离审计产物时必须显式指定隔离路径。

4. 当前 `recent_10_cache.json`只显式保存站点名称、URL、方向和数据向量，未完整保存解析规则、字段规则和配置指纹。正式判重仍必须重新核对当前 `sites.json`及有效配置快照，不能仅凭缓存文件存在就认定缓存有效。

5. 当前缓存包含 9 个失败站点和不完整向量。新增站判重服务可跳过它们并使用其余 231 个完整向量；正式全站连续检测会整体拒绝该缓存，因此当前只能报告全站连续检测未完成。

6. 当前正式重复检测 BAT 使用 `--use-recent-cache`，但未显式传入 `--no-write-cache`。只要缓存模式失效或入口行为发生变化，必须确保判重流程不会回写正式缓存；任何判重缓存写入都必须先经过明确授权。

7. `shawei/config/paths.py` 当前把成功、失败目录定义为项目根目录的同级目录，与本文件规定的 `每天工具\数据系列` 正式目录不一致。修复前不得执行会覆盖正式结果的流程，也不得把测试目录输出当成正式输出。

8. `find_existing_site()` 当前只检查名称和精确 URL；topic、文章 ID、栏目和字段身份仍需独立核对。新增站判重服务的 `accepted=True` 不包含用户对不足 10 期特例的授权。

9. `shawei_failed_site_validator.py` 当前只验证固定 21 站清单及其硬编码期数，不能代替任意失败站点的指定期、相邻期和不存在期完整验证。

## 规则证据

`新增站点检测规则.md`仅作为本项目已有流程证据，不得覆盖或放宽上级 `..\AGENTS.md` 的统一规则。
