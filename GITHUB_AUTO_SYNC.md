# GitHub 自动同步

- 远程：`origin` → `https://github.com/xiaobai156/weiweiwei`；分支：`main`。
- 每次修改完成并通过必要验证后，提交并推送本次任务改动。
- 只提交本次已验证的文件，保留用户已有未提交或已暂存改动；禁止使用 `git add .`、`git commit -a` 或等效全量暂存。
- 推送前排除凭据、Token、密码、临时文件、缓存、构建产物和无关项目。
- 验证失败、认证失败、网络失败、冲突或无法确认改动归属时，不提交、不推送，并报告原因。
