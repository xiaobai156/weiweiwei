# GitHub 自动同步规则

- 当前项目的远程仓库为 `https://github.com/xiaobai156/weiweiwei`，远程名为 `origin`，默认分支为 `main`。
- Codex 每次在当前项目完成修改并通过必要验证后，自动提交本次已完成的修改并推送到 `origin`。
- 只提交本次任务产生且已验证的改动；保留用户已有未提交或已暂存改动，不使用 `git add .`、`git commit -a` 或等效方式混入它们。
- 推送前排除凭据、Token、密码、临时文件、缓存、构建输出和无关项目；发现认证或敏感信息风险时停止推送并报告。
- 仅在必要验证通过后推送；验证失败或无法确认改动归属时不提交、不推送。
